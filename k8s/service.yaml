apiVersion: v1
kind: Service
metadata:
  name: ticket-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: ticket-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080  # ✅ Access via http://<minikube-ip>:30080
