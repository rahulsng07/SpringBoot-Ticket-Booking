apiVersion: v1
kind: Service
metadata:
  name: ticket-app-service
  namespace: ticket-system
spec:
  selector:
    app: ticket-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
